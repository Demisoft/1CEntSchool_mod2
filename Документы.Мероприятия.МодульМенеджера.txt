Функция УрокиДня(День) Экспорт 
	
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!

	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	Мероприятия.Ссылка КАК Урок,
		|	Мероприятия.Проведен КАК Был
		|ИЗ
		|	Документ.Мероприятия КАК Мероприятия
		|ГДЕ
		|	НАЧАЛОПЕРИОДА(Мероприятия.Дата, ДЕНЬ) = &День
		|	И Мероприятия.ТипМероприятия = ЗНАЧЕНИЕ(Справочник.ТипыМероприятий.Урок)";

	Запрос.УстановитьПараметр("День", День);

	Результат = Запрос.Выполнить();

//	ВыборкаДетальныеЗаписи = Результат.Выбрать();
//	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
//		// Вставить обработку выборки ВыборкаДетальныеЗаписи
//	КонецЦикла;
//	}}КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	
	Возврат Результат.Выгрузить();	
	
КонецФункции // УрокиДня()
